import Display from '../Display.js'

export default class Bar extends Display
{
	/**
	 * @param {HTMLElement} element 
	 * @returns {Bar}
	 */
	static from(element)
	{
		return super.from(element)
	}
}